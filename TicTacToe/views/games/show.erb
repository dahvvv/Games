<h1>Hi <%= current_user.username %>!  Here's your game!!</h1>
<h2>You are <%= current_user.x_or_o %>, and <span class="whose_turn"><%= (current_user.my_turn == true) ? "it's your turn!" : "it is not your turn." %></span></h2>

<div class="board">
  <% @game.boardspaces.each do |boardspace| %>
    <div class="boardspace myturn<%=current_user.my_turn%>" id="<%= boardspace.position %>">
    </div>
  <% end %>
</div>